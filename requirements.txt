# macOS (Apple Silicon) friendly requirements for "Hands-On Large Language Models"
#
# Notes for macOS arm64:
#   - bitsandbytes is omitted (CUDA-only at 0.43.1; use Colab for QLoRA / quantization chapters)
#   - llama-cpp-python should be installed separately with Metal support (see bottom)

# Data handling
numpy == 1.26.4
pandas == 2.2.2
datasets == 2.20.0

# Environment
jupyterlab == 4.2.2
ipywidgets == 8.1.3

# Hard dependencies throughout the entire book
torch == 2.3.1
transformers == 4.41.2
sentence-transformers == 3.0.1
matplotlib == 3.9.0
scikit-learn == 1.5.0
sentencepiece == 0.2.0
nltk == 3.8.1
evaluate == 0.4.2

# Cloud providers
openai == 1.34.0
cohere == 5.5.8

# Chapter-specific dependencies
datamapplot == 0.3.0
faiss-cpu == 1.8.0
bertopic == 0.16.3
annoy == 1.17.3
numexpr == 2.10.0
langchain == 0.2.5
langchain-community == 0.2.5
langchain-openai == 0.1.8
duckduckgo-search == 7.1.1
gensim == 4.3.3
setfit == 1.0.3
seqeval == 1.2.2
trl == 0.9.4
peft == 0.11.1
accelerate == 0.31.0
mteb == 1.12.39

# --- Skipped on macOS arm64 ---
# bitsandbytes == 0.43.1   # CUDA-only at this version; run quantization/QLoRA chapters on Colab
# llama_cpp_python == 0.2.78 -C cmake.args="-DLLAMA_BLAS=ON"   # install separately with Metal, see below

# After `pip install -r requirements.txt`, install llama-cpp-python with Metal:
#   CMAKE_ARGS="-DLLAMA_METAL=ON" pip install --no-cache-dir llama-cpp-python==0.2.78
